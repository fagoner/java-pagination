## Demo projecto for pagination

Simple project to create pagination object,
calculate `pageSize`, `page`, `rangeBegin`, `rangeEnd`


## To do 
- [ ] rest api to request dummy data using the model of pagination
